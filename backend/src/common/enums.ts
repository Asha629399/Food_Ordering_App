export enum Role {
  ADMIN = 'admin',
  MANAGER = 'manager',
  MEMBER = 'member',
}

export enum Country {
  INDIA = 'india',
  AMERICA = 'america',
}

export enum OrderStatus {
  PENDING = 'pending',
  PLACED = 'placed',
  CANCELLED = 'cancelled',
}
